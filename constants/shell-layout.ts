/**
 * Layout shell (drawer) — spaziatura coerente sotto la top bar.
 * Il contenuto delle screen non deve partire “attaccato” all’header.
 */
export const DRAWER_SCREEN_PADDING_TOP = 20;

/** Margine orizzontale unico per le schermate principali del drawer (Home, Appuntamenti, Stats). */
export const DRAWER_SCREEN_PADDING_HORIZONTAL = 22;
